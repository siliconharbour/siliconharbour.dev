---
id: s-5603
status: closed
deps: []
links: []
created: 2026-05-22T20:16:24Z
type: task
priority: 1
assignee: Jack Arthur Harrhy
parent: s-2aa6
tags: [backend, discord]
---
# Discord: destinations CRUD + config refactor

Add app/lib/discord-destinations.server.ts with listDestinations(type), addDestination({type, guildId, guildName, channelId, channelName}), removeDestination(id). Update config.server.ts DiscordConfig: drop eventsChannelId/jobsChannelId, keep botToken. Migrate old keys into destinations on first read (one-time, idempotent: if a value exists and no destination with that channelId exists for that type, create one with placeholder guild data, then delete the key).

## Acceptance Criteria

After migration, settings reads via getDiscordDestinations('events'). updateDiscordConfig only touches botToken. Existing posters compile against the new helpers.

